<div class="container">
  <%= link_to :back, class: "bt btn-back" do %>
  <% end %>
  <% @msg.each_line do |s| %>
    <h1><%= s %></h1>
  <% end %>
  <div class="card-grid">
    <% @cues.each do |cue| %>
      <% if cue.title == "Rainy day!"%>
        <%= link_to new_cue_user_cue_path(cue.id), class: "card-cue bg-barge" do %>
          <div class="link-to-text-color">
            <h2 class="emoji">🌧</h2>
            <h3><%= cue.title %></h3>
            <p><%= cue.description %></p>
          </div>
        <% end %>
      <% elsif cue.title == "Burger day!"%>
        <%= link_to new_cue_user_cue_path(cue.id), class: "card-cue bg-jade"  do %>
          <div class="text-center">
            <h1 class="emoji">🍔</h1>
            <h3><%= cue.title %></h3>
            <p><%= cue.description %></p>
          </div>
        <% end %>
      <% elsif cue.title == "Big spenda!"%>
        <%= link_to new_cue_user_cue_path(cue), class: "card-cue bg-purple"  do %>
          <div class="text-center">
            <h1 class="emoji">💸</h1>
            <h3><%= cue.title %></h3>
            <p><%= cue.description %></p>
          </div>
        <% end %>
      <% elsif cue.title == "Starbucks"%>
        <%= link_to new_cue_user_cue_path(cue), class: "card-cue bg-coffee"  do %>
          <div class="text-center">
            <h1 class="emoji">☕️</h1>
            <h3><%= cue.title %></h3>
            <p><%= cue.description %></p>
          </div>
        <% end %>
      <% end %>
    <% end %>
  </div>
</div>
