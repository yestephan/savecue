<div class="container">
  <div class="inner-container no-margin">
   <%if current_user.accounts.count >= 2%>
   <%= link_to link_back, class: "bt btn-back" do %>
   <% end %>
    <% end %>
    <h1><%= info_for_category(@cue.category) %></h1>
    <%= simple_form_for [@cue, @user_cue] do |f| %>
      <div class="form-floating">
        <%= f.input :cue_amount,
                      label: 'Amount',
                      required: true,
                      class: "form-control",
                      placeholder: "€" %>
      </div>
      <%= f.button :submit, @submit_title, class: "bt btn-primary" %>
    <% end %>
  </div>
  <%= render 'shared/flashes' %>
</div>
